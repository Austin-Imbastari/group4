import React from "react";
import styled from "styled-components";

const CreateEvent = () => {
  return (
    <div>
      <h1>CreatEvent page</h1>
    </div>
  );
};

const Wrapper = styled.div`
  height: 100vh;
`;

export default CreateEvent;
