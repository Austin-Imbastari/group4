# Elderly

React & Vite project

```bash
# 1) Create a new Vite + React app
npm create vite@latest my-app -- --template react

cd my-app

# 2) Install deps
npm install

# 3) Start dev server
npm run dev
```
